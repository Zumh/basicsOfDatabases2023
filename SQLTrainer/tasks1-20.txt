# tasks 1 Select for the titles of all movies
SELECT name FROM movies;
SELECT nimi FROM Elokuvat;

# tasks 2 Get the titles and release years of all movies
SELECT name, year FROM Elokuvat;

# tasks 3 Get the 
